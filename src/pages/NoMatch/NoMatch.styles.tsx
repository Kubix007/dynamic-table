import { Typography } from "@mui/material";
import styled from "styled-components";

export const NoMatchInfo = styled(Typography)`
  &.MuiTypography-root {
    font-family: FiraSans-Light;
    font-size: 2em;
    text-align: center;
  }
`;
